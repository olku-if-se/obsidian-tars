{
  "name": "@tars/types",
  "version": "3.6.0",
  "description": "TypeScript type definitions for Tars",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:watch": "vitest --watch --coverage --ui",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "check": "biome check --write .",
    "clean": "rimraf dist",
    "quality": "pnpm run typecheck && pnpm run lint && pnpm run test && pnpm run build",
    "quality-check": "pnpm run typecheck && pnpm run lint && pnpm run test",
    "knip": "knip"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.0",
    "rimraf": "^6.0.1",
    "tsup": "8.5.0",
    "typescript": "5.9.3",
    "vitest": "4.0.3",
    "knip": "^5.66.2"
  },
  "publishConfig": {
    "access": "public"
  }
}
